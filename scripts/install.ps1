# LSP-IO CLI installer placeholder (Windows)
#
# Release download URLs are not active until the first public LSP-IO release.

$ErrorActionPreference = "Stop"

Write-Host "LSP-IO does not have published release artifacts yet." -ForegroundColor Yellow
Write-Host "Build locally with:" -ForegroundColor Cyan
Write-Host "  cargo build --release -p lsp-io-cli"
